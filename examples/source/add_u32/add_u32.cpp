/// note that in cpp, the addition of two unsigned integers (or unsigned integer arithmetic)
/// is always well-defined with potential wrapping behavior.
unsigned int add(unsigned int a, unsigned int b) {
    return a + b;
}
